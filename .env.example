# Copy to `.env` and add required keys.
PYTHONUTF8=1
PYTHONIOENCODING=utf-8
# OPENAI_API_KEY=sk-...
# Optional: only if you use Chroma tools outside this crew config (embeddings use OPENAI_API_KEY via api_key_env_var).
# CHROMA_OPENAI_API_KEY=sk-...
# PUSHOVER_USER=...
# PUSHOVER_TOKEN=...
